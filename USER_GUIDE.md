# 秘書AI ユーザーガイド

「Google ログインだけで始められる秘書AI」のセットアップ手順です。
所要 5〜10分。Gemini API キーの取得を含みます。

---

## 🎯 全体の流れ

```
1. 秘書AIのサイトを開く
   ↓
2. Google ログイン
   ↓
3. 呼ばれたい名前 + Gemini API キー設定 (自動ガイド)
   ↓
4. ホーム画面で秘書と会話
   ↓
5. (任意) Chrome 拡張機能をインストールしてタイマー＆集中モード
   ↓
6. (任意) スマホに ntfy アプリを入れてプッシュ通知
```

---

## ① 秘書AIにログインする

1. `https://secretary-ai-next.vercel.app/` をブラウザで開く
2. 「Google でログイン」をクリック
3. 同意画面で **Google カレンダー・Google タスク・Google Drive(読み取り)・Gmail(読み取り)** へのアクセスを許可
   - すべてあなたのデータをあなた専用で読み書きするためです
   - 他のユーザーや運営はあなたのデータを見ません

## ② オンボーディングを完了する

初回ログイン後、自動で **3ステップのオンボーディング**が始まります。

### Step 1: 呼ばれたい名前を入力

秘書から会話の中でこう呼ばれます。本名・ニックネーム・あだ名なんでもOK。

### Step 2: Gemini API キーを取得して入力

これが必須です。**自分専用のキー**を Google から無料で取得します。

```
1. https://aistudio.google.com/apikey にアクセス
2. Google ログイン
3. 「Create API key」をクリック
4. 「Create API key in new project」を選択(初回)
5. 表示された「AIzaSy...」で始まる文字列をコピー
6. 秘書AIのオンボーディング画面に貼り付け
```

#### Gemini の無料枠について

- **1日1,000回まで無料**（毎日リセット）
- 個人で普通に使う分には超えません（朝・夜・チャットで多くて1日100回程度）
- 超えても課金されない設定がデフォルト

### Step 3: 完了！

「ホームへ」を押すと、秘書AIのダッシュボードに入ります。

## ③ 秘書と会話する

ホーム画面の中央にチャット欄があります。試しに：

- 「今日の時間割を組んで」
- 「○○をやらなきゃ」（タスク追加のヒアリング）
- 「○○を消しといて」（タスク削除）
- 「明日の14:00に新宿で打ち合わせ入れて」（カレンダーに予定追加）

普通の言葉で話せばOK。

## ④ Chrome 拡張機能をインストール（任意・推奨）

「集中モード ON で他サイトをブロック、タブを開いてもタイマーが見える」機能です。

### インストール手順

1. 配布された `focus-secretary-extension.zip` を**好きな場所に展開**（例: `C:\Users\xxx\Documents\focus-secretary`）
2. Chrome のアドレスバーに `chrome://extensions/` と入力して開く
3. 右上の「**デベロッパーモード**」をオンにする
4. 左上の「**パッケージ化されていない拡張機能を読み込む**」をクリック
5. 展開したフォルダを選択
6. 拡張機能リストに「Focus Secretary」が追加される

### 使い方

1. Chrome ツールバーの **拡張機能アイコン**から「Focus Secretary」をピン留め
2. アイコンをクリック → popup が開く
3. 「📌 サイドパネルを開く」で秘書AIをブラウザの右側に常駐表示
4. 「🎯 集中モード」をONにすると、ブロックリストのサイト（X / YouTube / Threads など）が**集中タイム中ブロック**される + 自動でポモドーロ(30分 + 5分休憩)スタート
5. 「⏰ 今日の時間割」に時間割を書いて保存すると、**どのタブを開いても画面右下に「いま: ○○」が常時表示**

## ⑤ スマホ通知を設定する（任意）

朝/夜の声かけ、集中タイム終了などの通知をスマホで受け取りたい場合。

### 手順

1. スマホで **ntfy アプリ**をインストール
   - iOS: https://apps.apple.com/jp/app/ntfy/id1625396347
   - Android: https://play.google.com/store/apps/details?id=io.heckel.ntfy
2. 推測されにくいトピック名を考える（例: `mysecret_yourname_8421ab`）
3. ntfy アプリで「Subscribe to topic」 → 上のトピック名を入力 → Subscribe
4. 秘書AIの「⚙️ 設定 → 📱 スマホ通知」に同じトピック名を貼り付けて保存

朝(8時)・夜(20時)・集中タイム関連の通知がスマホに届くようになります。

---

## 🆘 困ったとき

| 症状 | 対処 |
|---|---|
| 「This page couldn't load」が出る | 強制リロード `Ctrl+Shift+R`、それでもダメならシークレットウィンドウで開く |
| 秘書が反応しない/エラー | Gemini API キーが正しいか「⚙️ 設定」で確認 |
| タスクが消えない | 「○○のタスク消して」と明示的に頼む |
| カレンダー予定が表示されない | Google ログインで「Calendar の読み取り」が許可されているか確認 |
| Chrome 拡張でバッジが出ない | 拡張をリロード後、ページも Ctrl+R で再読み込み。`chrome://newtab/` のような Chrome 内部ページでは出ません |

## 📨 不具合報告

GitHub Issues か、運営者に直接 DM ください。
