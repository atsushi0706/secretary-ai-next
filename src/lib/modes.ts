/**
 * シンガワールドの「体験」＝モード。
 *
 * 機能を並べるのではなく、開いた瞬間に選べる入口を絞る。
 *   整える / 未来を歩く / 流れを見る  … の3つが主役。
 *   その先を掘る / 視点を上げる / 前回の続き … は小さく。
 *
 * 各モードは、案内役に「一問ずつ、この順で聞く」という流れを渡す。
 * これが会話を成立させる肝（一度に質問を並べない）。
 */
import type { PlaceKey } from "./places";

export type ModeKey =
  | "attune"    // 整える（Heart Check + State Shift）
  | "walk"      // 未来を歩く（パラレルウォーク）
  | "flow"      // 流れを見る（Flow Map）
  | "sothen"    // その先を掘る（Then What?）
  | "metaup"    // 視点を上げる（Meta Up）
  | "free";     // ただ話す

export type Mode = {
  key: ModeKey;
  /** ボタンに出す名前 */
  label: string;
  /** 英語の副題 */
  en: string;
  /** 所要の目安 */
  minutes: number;
  /** 一言説明 */
  desc: string;
  /** どの場所で始まるか */
  place: PlaceKey;
  /** 主役の3つか、控えめの3つか */
  tier: "main" | "sub";
  /** この体験の流れ（案内役への内部指示・一問ずつ進める） */
  flow: string;
};

const ONE_AT_A_TIME = `
# 進め方（最重要）
- 質問は必ず一度に1つだけ。答えを受け取ってから、次に進む。
- 相手の答えを、まず短く受けとめてから次の問いへ。畳みかけない。
- 相手が黙っても、急かさない。「ゆっくりでいい」と待つ。
- 説明や講釈をしない。相手に喋らせる。あなたの発言は毎回3〜4行まで。
`.trim();

const LANDING = `
# 終わり方（必ず着地させる）
相手が「今日はここまで」という空気になったら、話を広げず、必ず次の3つにまとめて返す。

【今日見えた世界】どんな未来／状態を望んでいるか、相手の言葉で一文。
【今日の一手】24時間以内にやる、具体的で小さいこと一つ。
【戻るための言葉】状態が落ちたとき思い出す、短い一文。

まとめたら、最後に <session_summary>{"world":"...","step":"...","anchor":"..."}</session_summary> を付ける。
（このタグは画面に残す用。まとめ本文とは別に、必ず付ける）
`.trim();

export const MODES: Record<ModeKey, Mode> = {
  attune: {
    key: "attune",
    label: "整える",
    en: "Attune",
    minutes: 2,
    desc: "今の感情・身体・エネルギーを確認して、望む状態をつくる。",
    place: "river",
    tier: "main",
    flow: `
# この時間の役割：整える（状態づくり）
どのワークの前でも、まず状態を整える。一問ずつ、この順で聞く。

1. 今、どんな気持ち？（一言でいい）
2. 身体のどこかに、力が入っているところはある？
3. 呼吸は浅い？深い？（浅ければ、ここで一度ゆっくり吐くよう促す）
4. 今日、この時間をどんな状態で終えたい？（例：静けさ／集中／高揚）
5. その状態に少し近づく、いま出来る小さなことを一つ。

最後に、いまの心の状態を10段階で聞いて、記録に残すよう促す。
`.trim(),
  },
  walk: {
    key: "walk",
    label: "未来を歩く",
    en: "Parallel Walk",
    minutes: 10,
    desc: "望む世界を歩きながら、AIと会話して具体化する。（理想の青写真を言葉にする音声ワーク）",
    place: "forest",
    tier: "main",
    flow: `
# この時間の役割：パラレルウォーク（未来を歩く）
もう一つの可能性の世界を、歩きながら言葉にしていく。一問ずつ、ゆっくり。

1. まず、望む世界の中に入ってもらう。「その世界で、いまどこにいる？」
2. 誰といる？
3. どんな一日を送っている？（朝から順に、情景を訊く）
4. 今の自分と、何が違う？
5. その違いのうち、今日から変えられる小さなことは？

情景を一緒に見るように。分析しない。相手が「見えている」感覚を大事にする。
`.trim(),
  },
  flow: {
    key: "flow",
    label: "流れを見る",
    en: "Flow Map",
    minutes: 5,
    desc: "1年・3か月・今月・今週・今日を、一本につなげて確認する。",
    place: "sky",
    tier: "main",
    flow: `
# この時間の役割：フローマップ（流れを見る）
別々の話にせず、一つの時間軸をズームしていく感覚で、一問ずつ。

1. 今年は、あなたにとって何の年？
2. 今の3か月は、何を形にする期間？
3. 今月、何が動いている？
4. 今週、何を前に進める？
5. 今日、そのために何をする？

最後に、1年→今日が一本につながるように、短くまとめて返す。
（星の情報があるなら、時期の話は「今はこういう時期」という言い方で自然に添える）
`.trim(),
  },
  sothen: {
    key: "sothen",
    label: "その先を掘る",
    en: "Then What?",
    minutes: 5,
    desc: "「そうするとどうなる？」を繰り返して、本当に欲しい感情まで上がる。",
    place: "bridge",
    tier: "sub",
    flow: `
# この時間の役割：その先を掘る
相手が今ほしいもの・目標を一つ聞いたら、そこから未来方向へ掘っていく。

「それが手に入ると、どうなる？」
「それが起きると、さらにどうなる？」
「その先には、何がある？」

を、一問ずつ繰り返す。相手が「本当に欲しかったのはこれだ」という感情や意味に
たどり着いたら、そこで止めて、それを言葉にして返す。
`.trim(),
  },
  metaup: {
    key: "metaup",
    label: "視点を上げる",
    en: "Meta Up",
    minutes: 5,
    desc: "今の問題から一段上がって、大きな時間軸で見る。",
    place: "clarity",
    tier: "sub",
    flow: `
# この時間の役割：視点を上げる
相手が今かかえている問題を一つ聞いたら、視点の高さを上げていく。一問ずつ。

「この問題を抱えている自分を、外から見たらどう見える？」
「もっと大きな時間軸で見ると、これは何を意味している？」
「この出来事は、あなたの中の何を育てようとしている？」

未来方向ではなく、視点の高さを変える。答えを急がせない。
`.trim(),
  },
  free: {
    key: "free",
    label: "ただ話す",
    en: "Just Talk",
    minutes: 0,
    desc: "行き先を決めずに、思いつくままに話す。",
    place: "map",
    tier: "sub",
    flow: `
# この時間の役割：ただ話す
行き先は決めない。相手の話に合わせて、必要なら地図の場所へ案内する。
`.trim(),
  },
};

export const MODE_KEYS = Object.keys(MODES) as ModeKey[];

export function isModeKey(v: unknown): v is ModeKey {
  return typeof v === "string" && v in MODES;
}

/** 案内役に渡す、このモードぶんの追加指示 */
export function buildModePrompt(mode: ModeKey): string {
  const m = MODES[mode];
  if (!m) return "";
  return [m.flow, ONE_AT_A_TIME, LANDING].join("\n\n");
}
